# Contributing

Use small, focused changes. Run `npm run typecheck`, `npm run test`, and `pytest apps/api` before opening a pull request. Include a migration for every persistent-schema change and update the API contract when endpoints change.
