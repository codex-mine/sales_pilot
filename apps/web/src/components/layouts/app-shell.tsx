"use client";
import Link from "next/link";
import { Bell, LayoutDashboard, Menu, Settings, Users } from "lucide-react";
import { useState, type ReactNode } from "react";
const navigation = [{ href: "/dashboard", label: "Dashboard", icon: LayoutDashboard }, { href: "/settings", label: "Settings", icon: Settings }, { href: "/team", label: "Team", icon: Users }];
export function AppShell({ children }: { children: ReactNode }): React.ReactElement { const [open, setOpen] = useState(false); return <div className="min-h-screen md:flex"><aside className={`${open ? "block" : "hidden"} w-64 border-r bg-white p-4 dark:bg-slate-900 md:block`}><Link href="/dashboard" className="mb-8 block text-xl font-bold">SalesPilot</Link><nav className="space-y-1">{navigation.map(({ href, label, icon: Icon }) => <Link key={href} href={href} className="flex items-center gap-3 rounded-md px-3 py-2 text-sm hover:bg-slate-100 dark:hover:bg-slate-800"><Icon size={17} />{label}</Link>)}</nav></aside><main className="min-w-0 flex-1"><header className="flex h-16 items-center justify-between border-b bg-white px-4 dark:bg-slate-900"><button className="md:hidden" onClick={() => setOpen(!open)} aria-label="Toggle navigation"><Menu /></button><div className="text-sm text-slate-500">Workspace / Overview</div><div className="flex items-center gap-4"><button aria-label="Notifications"><Bell size={18} /></button><div className="rounded-full bg-indigo-100 px-3 py-1 text-sm font-medium text-indigo-700">You</div></div></header><div className="p-4 md:p-8">{children}</div></main></div>; }
